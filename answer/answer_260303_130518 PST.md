# Azure RBAC Answer — Pre-create log/ and answer/ runtime directories

> Generated by Azure RBAC Advisor from `resources/` reference library.
> Prompt logged: `log/copilot-log_260303_130518 PST.md`
> Sources: N/A (infrastructure setup task, no RBAC resource files consulted)

---

## Task Completed

Both runtime directories have been pre-created in the repository root:

| Directory | File | Purpose |
|-----------|------|---------|
| `log/` | `log/.gitkeep` | Tracks the `log/` directory in git; all `*.md` log files are excluded via `.gitignore` |
| `answer/` | `answer/.gitkeep` | Tracks the `answer/` directory in git; all `*.md` answer files are excluded via `.gitignore` |

### Supporting Files Created/Updated

| File | Change | Purpose |
|------|--------|---------|
| `.gitignore` | Updated | `log/*.md` and `answer/*.md` excluded; `.gitkeep` files are tracked |
| `setup.sh` | Created | Post-clone script: `mkdir -p log answer && touch log/.gitkeep answer/.gitkeep` |
| `Makefile` | Created | `make setup` target — same as `setup.sh` |
| `README.md` | Updated | Added "First-Time Setup" section with `bash setup.sh` / `make setup` instructions |
| `.github/agents/azure-rbac-advisor.agent.md` | Updated | Added `bash` to tools list; added `mkdir -p log answer` auto-setup step |

### Pending Action

A manual git commit is required to persist these changes to GitHub. Run from the VS Code terminal:

```bash
cd /home/devcontainers/azure-rbac-agent-repo && \
git add -A && \
git commit -m "Pre-create log/ and answer/ runtime directories

- Add log/.gitkeep and answer/.gitkeep so directories exist in fresh clones
- Update .gitignore: log/*.md and answer/*.md excluded, .gitkeep tracked
- Add bash to agent tools list + mkdir -p auto-setup on session start
- Add setup.sh and Makefile (make setup) for post-clone dir recreation
- Update README with First-Time Setup section

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>" && \
git push origin main
```
